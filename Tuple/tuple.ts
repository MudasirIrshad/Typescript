// tuple is just assigning the items and their data types to array.

let tup:[string,number]=["mudasir",20]
console.log(`Name: ${tup[0]}`);
console.log(`Age: ${tup[1]}`);

// we can not assign more than 2 values to this tup[].